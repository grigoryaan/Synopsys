#include <iostream>

int main() {
     long long bits = 0x0010000000000000; // double-i amenapoqr normalacvac drakan tivy
    double min_d = *reinterpret_cast<double*>(&bits); // bitery poxum enq sarqum double tiv

    std::cout << "minimal double =  " << min_d << std::endl;
}
